# Handshake Agent-Aware Resolver

This project is for building an agent-aware resolver that works with any Handshake name that publishes compatible agent identity metadata.

The goal is to build a separate resolver layer without modifying hnsd core.